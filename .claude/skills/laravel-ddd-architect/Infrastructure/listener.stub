<?php

declare(strict_types=1);

namespace App\Infrastructure\{{BoundedContext}}\Event\Listener;

use App\Domain\{{BoundedContext}}\Event\{{EventName}};
use Illuminate\Contracts\Queue\ShouldQueue;

final class {{ListenerName}} implements ShouldQueue
{
    public int $tries = 3;
    public int $backoff = 30;

    public function handle({{EventName}} $event): void
    {
        // delegate to an Action; no business logic in listeners
    }
}

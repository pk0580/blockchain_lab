<?php

declare(strict_types=1);

namespace App\Application\{{BoundedContext}}\UseCase\{{UseCaseName}};

use App\Domain\{{BoundedContext}}\Repository\{{RepositoryName}};
use Illuminate\Contracts\Events\Dispatcher;
use Illuminate\Database\DatabaseManager;

final readonly class {{UseCaseName}}Action
{
    public function __construct(
        private {{RepositoryName}} $repository,
        private DatabaseManager $db,
        private Dispatcher $events,
    ) {}

    public function handle({{DTO}} $data): void
    {
        $this->db->transaction(function () use ($data) {
            // 1. Load or create the aggregate.
            // 2. Invoke business intent methods.
            // 3. Persist via repository.
            // 4. Dispatch domain events with $this->db->afterCommit(...).
        });
    }
}
